---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "sampleapp-service"
  namespace: "default"
spec:
  ports:
  - protocol: "TCP"
    port: 80
  selector:
    app: "sampleapp"
  type: "LoadBalancer"
  loadBalancerIP: ""
